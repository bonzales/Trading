[Unit]
Description=Bot giornaliero mean-reversion (paper) — un'esecuzione
After=ib-gateway.service
Wants=ib-gateway.service

[Service]
Type=oneshot
User=__USER__
WorkingDirectory=__REPO__
ExecStart=__REPO__/.venv/bin/python -m src.live.paper_bot
# non usa --dry-run: opera sul conto paper. Log in raw/live_log.csv
