# Esperimenti

Una pagina per ogni run di backtest significativo. Usa il template
`../_templates/experiment.md`. Le pagine vengono create dall'agente durante l'ingest
(vedi CLAUDE.md §3.1). Nessun esperimento OANDA ancora eseguito.
